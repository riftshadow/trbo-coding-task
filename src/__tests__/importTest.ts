describe('import', () => {
    it('fails import when fields are missing or file is empty', () => {

    });

    it('wrong file format send via post', () => {

    });

    it('delimiter which is defined could not be fined', () => {

    });

    it('assert true when file and content could be parsed and imported', () => {

    });
});
