describe('sell endpoint test', () => {
    it('invalid parameters used for sell endpoint', () => {

    });

    it('invalid quantity sent, negative numbers, NaN values', () => {

    });

    it('valid sku and quantity, order and order_position are created => 200', () => {

    });
});
